---
workflows:
  myworkflow:
    plan:
      steps:
      - run: echo "Custom Workflow"
      - init
      - plan
      - run: echo "Run checkov"
    apply:
      steps:
      - apply
      - run: echo "Send webhook slack for example"
